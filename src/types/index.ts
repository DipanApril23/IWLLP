// Public types, re-exported from the data layer so components can import a
// type without reaching for a specific loader file.
export type {
  Service,
  ServiceIcon,
  NavLink,
  HeroSlide,
  HeroCta,
  ContactField,
  SiteConfig,
} from "@/data";
